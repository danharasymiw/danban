package components

import (
"fmt"
"github.com/danharasymiw/danban/server/store"
)

templ CardComponent(card *store.Card) {
<div id={ fmt.Sprintf("card-%s", card.Id) } class="bg-white p-2 m-2 rounded-md shadow-sm">
  <div class="text-md text-ellipsis break-word">{ card.Title }</div>
</div>
}
